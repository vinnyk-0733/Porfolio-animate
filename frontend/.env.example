# Copy to .env.local and configure private server-side values.
# Use the same database for the deployed website and password setup script.
MONGODB_URI=
MONGODB_DB=portfolio_db

# No authentication/session secret is needed for password-only edit mode.
